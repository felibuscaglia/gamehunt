<!doctype html>
<html lang="en">
   <head>
      <%- include('./shared/head.ejs', { title: 'Verification Email' }) %>
      <style>
        .content {
      padding: 24px;
      background-color: white;
      width: 60vw;
      display: flex;
      flex-direction: column;
      gap: 30px;
    }

    .confirm-btn {
      background-color: #7e57c2;
      color: white;
      width: 100%;
      font-size: 15px;
      padding: 20px 0;
      border: none;
      border-radius: 4px;
      display: block;
      text-align: center;
    }

    .link {
      color: #7e57c2;
      word-wrap: break-word;
    }
      </style>
   </head>
   <body>
    <div class="content">
      <p>
        Hey there, <strong><%= full_name %></strong><br /><br />
  
        We've noticed that you haven't confirmed your email address associated with your <%= app_name %> account
        <strong>@<%= username %></strong>. Please click on the button or link below to confirm your email.
      </p>
      <a class="confirm-btn" href="<%= confirmation_url %>">Confirm my account</a>
      <a class="link" href="<%= confirmation_url %>"><%= confirmation_url %></a>
      <p>
        If you didn't request this, please ignore this email. <br /><br />
      Cheers,<br />
      The <%= app_name %> team
      </p>
    </div>
   </body>
</html>