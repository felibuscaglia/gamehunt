<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <%- include('./shared/head.ejs', { title: 'Reset Password' }) %>
    <style>
      .content {
        padding: 24px;
        background-color: white;
        width: 60vw;
        display: flex;
        flex-direction: column;
        gap: 15px;
      }

      a {
        background-color: #7e57c2;
        color: white;
        width: 100%;
        font-size: 15px;
        padding: 20px 0;
        border: none;
        border-radius: 4px;
        display: block;
        text-align: center;
      }
    </style>
  </head>
  <body>
    <div class="content">
      <p>
        A password change has been requested for your account. If this was you,
        please use the link below to reset your password.
      </p>
      <a target="_blank" rel="noreferrer" href="<%= reset_password_url %>"
        >Reset password</a
      >
    </div>
  </body>
</html>
