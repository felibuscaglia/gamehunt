export enum USER_ROLES {
  USER = "User",
  ADMIN = "Admin",
}

export enum TEXT_SIZE {
  SMALL = "text-sm",
  BASE = "text-base",
}

export enum GamePricing {
  FREE = "free",
  PAID = "paid",
  PAID_WITH_FREE_TRIAL = "paid-with-free-plan",
}

export enum GameCreationSidebarSectionIndexes {
  MAIN_INFO,
  LINKS,
  MEDIA,
  CREATORS,
}
